<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.insyaallahsukses.misan.belanjaancustomer">

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/DefaultTheme">
        <activity
            android:name=".MainActivity"
            android:theme="@style/DefaultTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".DaftarPengguna" />
        <activity android:name=".LoginPengguna" />
        <activity
            android:name=".HistoriPesananPengguna"
            android:label="Histori Pesanan Barang" />
        <activity
            android:name=".KonfirmasiPesanan"
            android:label="Konfirmasi Pesanan" />
        <activity android:name=".Cart" />
        <activity android:name=".StatusPesanan" />
        <activity android:name=".ListTokoTerdekat" />
        <activity
            android:name=".DetailToko"
            android:theme="@style/DefaultTheme"></activity>
    </application>

</manifest>